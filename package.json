{
  "name": "medium-stats",
  "version": "0.0.1",
  "description": "Renders medium stats",
  "keywords": ["medium", "stats"],
  "license": "MIT",
  "author": "",
  "scripts": {
    "build": "parcel build --public-url '/medium-earnings' index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
